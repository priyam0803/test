version: 2

models:
    - name: model_2
      description: "A state wise order count"
      columns:
          - name: STATE_NAME
            description: "The state name for this table"
            tests:
                - unique
                - not_null
